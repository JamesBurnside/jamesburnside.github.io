import * as React from "react";
import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export const Footer = () => {

  return (
    <Box sx={{ flexGrow: 1, borderTop: 1, borderColor: "divider", textAlign: "center", minHeight: '2rem' }} mt={'2rem'} role="footer">
    </Box>
  );
};
